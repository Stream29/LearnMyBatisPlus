spring:
  application:
    name: LearnMyBatisPlus
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/learn_mybatis_plus?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: password

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
